{
  "name": "@iris-extension/web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ../../script/prepare-extension-sdk.mjs && bun build ./src/index.ts --outfile ./dist/index.mjs --target=node --format=esm --packages=bundle --external node-pty && bun ../../script/validate-extension-bundle.ts --file ./dist/index.mjs --extension web --allow node-pty",
    "build:ui": "cd web-ui && npm run build"
  },
  "dependencies": {
    "irises-extension-sdk": "file:../../packages/extension-sdk",
    "ws": "^8.18.0",
    "node-pty": "^1.1.0",
    "yaml": "^2.7.0"
  }
}
